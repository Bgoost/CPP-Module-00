#ifndef UTILS_HPP
# define UTILS_HPP

# include <iostream>
# include <string>

class Utils
{
    public:
        Utils();
        ~Utils();
        // int is_alnum(std::string str);
        // bool is_alpha(const std::string &str);
        // int is_num(std::string str);
        // int check_space(std::string str);
    
};

#endif